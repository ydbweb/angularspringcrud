# MySQL Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/sakilacrud?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.hibernate.ddl-auto=update

# JPA and Hibernate Configuration
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect # Or appropriate dialect for your MySQL version
spring.jpa.hibernate.ddl-auto=update # 'update' to create/update tables, 'none' for production
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Server Port
server.port=8080

# CORS Configuration for Angular Frontend
spring.mvc.cors.enabled=true